(function e(t,n,r){function s(o,u){if(!n[o]){if(!t[o]){var a=typeof require=="function"&&require;if(!u&&a)return a(o,!0);if(i)return i(o,!0);var f=new Error("Cannot find module '"+o+"'");throw f.code="MODULE_NOT_FOUND",f}var l=n[o]={exports:{}};t[o][0].call(l.exports,function(e){var n=t[o][1][e];return s(n?n:e)},l,l.exports,e,t,n,r)}return n[o].exports}var i=typeof require=="function"&&require;for(var o=0;o<r.length;o++)s(r[o]);return s})({1:[function(require,module,exports){
'use strict';

// global
var adminPrefix = 'admin@ip-109-45-89-458:~$ ';
var sectionVisibleClass = 'admin-section--visible';
var sectionHiddenClass = 'admin-section--hidden';
var menuFocusedClass = 'menu--focused';
var menuContainer = document.querySelector('#menu');
var menuInput = document.querySelector('#menu-input');

var activeSection = void 0;

var addFeedback = function addFeedback(selector, content) {
	document.querySelector(selector).innerHTML += adminPrefix + content + '<br/>';
};

// nav
var adminSections = Array.prototype.slice.call(document.querySelectorAll('.admin-section'));

// nav click
var navLinks = Array.prototype.slice.call(document.querySelectorAll('#menu a'));

var navTargets = navLinks.map(function (n) {
	return n.getAttribute('href').replace('#', '');
});

var showSection = function showSection(selector) {
	var targetSection = document.querySelector(selector);

	adminSections.forEach(function (s) {
		if (s != targetSection) {
			s.classList.add(sectionHiddenClass);
			s.classList.remove(sectionVisibleClass);
			s.removeAttribute('tabindex');
		}
	});

	targetSection.classList.add(sectionVisibleClass);
	targetSection.classList.remove(sectionHiddenClass);
	targetSection.setAttribute('tabindex', '0');
	menu.classList.remove(menuFocusedClass);
	targetSection.querySelector('.admin-input').focus();

	menuInput.innerHTML = '';
	activeSection = selector.replace('#', '');
	addFeedback('#menu .feedback', selector.replace('#', ''));

	setDate(selector + ' .login-date');
};

navLinks.forEach(function (n) {
	return n.addEventListener('click', function (e) {
		e.preventDefault();
		var target = e.target.getAttribute('href');

		showSection(target);
	});
});

// nav type
menuInput.addEventListener('keypress', function (e) {
	if (e.keyCode === 13) {
		e.preventDefault();
		var content = e.target.textContent;

		if (navTargets.indexOf(content) >= 0) {
			showSection('#' + content);
		} else {
			addFeedback('#menu .feedback', content + '<br/>-bash: ' + content + ': command not found');
			menuInput.innerHTML = '';
		}
	}
});

menuInput.addEventListener('focus', function (e) {
	menu.classList.add(menuFocusedClass);
});

// undo nav type focus
menu.addEventListener('click', function (e) {
	if (e.target.tagName.toLowerCase() !== 'a') {
		menu.classList.add(menuFocusedClass);
		menuInput.focus();
	}
});

adminSections.forEach(function (s) {
	s.addEventListener('focus', function () {
		return menu.classList.remove(menuFocusedClass);
	});
	s.addEventListener('click', function () {
		return menu.classList.remove(menuFocusedClass);
	});

	// bind individual section inputs
	var commandLinks = Array.prototype.slice.call(s.querySelectorAll('a'));
	var commands = commandLinks.map(function (a) {
		return a.textContent.replace('--', '');
	});

	s.querySelector('.admin-input').addEventListener('keypress', function (e) {
		if (e.keyCode === 13) {
			e.preventDefault();
			var content = e.target.textContent;
			var command = content.split('--')[1];

			if (commands.indexOf(command) >= 0) {
				addFeedback('#' + s.id + ' .feedback', '' + content);
			} else {
				addFeedback('#' + s.id + ' .feedback', content + '<br/>-bash: ' + content + ': command not found');
			}
			e.target.innerHTML = '';
		}
	});

	commandLinks.forEach(function (c) {
		return c.addEventListener('click', function () {
			addFeedback('#' + s.id + ' .feedback', activeSection + ' ' + c.textContent);
		});
	});
});

// for displaying dates on windows
var setDate = function setDate(selector) {
	var stringDate = new Date().toString().split(' GMT')[0];
	document.querySelector(selector).innerHTML = stringDate;
};

setDate('#menu .login-date');

},{}]},{},[1]);
